import { Card, Table } from 'react-bootstrap'

type Props = {}

const Grades = (props: Props) => {
  return (
    <Card>
        <Table>
            burayı component yap
        </Table>
    </Card>
  )

}
export default Grades